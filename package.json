{
  "name": "@lakera/openclaw-plugin",
  "version": "0.1.0",
  "description": "OpenClaw plugin: screen tool calls with Lakera Guard and block if flagged",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  },
  "openclaw": {
    "extensions": ["./index.ts"]
  }
}
